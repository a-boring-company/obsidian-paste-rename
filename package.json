{
	"name": "obsidian-paste-rename",
	"version": "2.0.0",
	"description": "Rename pasted images and other attachments in Obsidian.",
	"main": "main.js",
	"scripts": {
		"start": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && BUILD_ENV=production node esbuild.config.mjs && cp manifest.json attachment-types.json build",
		"lint": "npm exec -- eslint src tests --ext .ts",
		"typecheck": "tsc --noEmit --skipLibCheck",
		"test": "vitest run",
		"coverage": "vitest run --coverage",
		"check": "npm run lint && npm run typecheck && npm test && npm run coverage && npm run build",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"release": "npm run build && gh release create ${npm_package_version} build/*"
	},
	"engines": {
		"node": ">=24 <25"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/a-boring-company/obsidian-paste-rename.git"
	},
	"bugs": {
		"url": "https://github.com/a-boring-company/obsidian-paste-rename/issues"
	},
	"homepage": "https://github.com/a-boring-company/obsidian-paste-rename#readme",
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"attachments",
		"rename"
	],
	"author": "A Boring Company",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^24.10.4",
		"@typescript-eslint/eslint-plugin": "^5.49.0",
		"@typescript-eslint/parser": "^5.49.0",
		"@vitest/coverage-v8": "3.2.4",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.27.2",
		"obsidian": "^1.11.0",
		"tslib": "2.5.0",
		"typescript": "^5.9.3",
		"vitest": "3.2.4"
	},
	"dependencies": {
		"cash-dom": "^8.1.2"
	}
}
